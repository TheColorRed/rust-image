export global DialogState {
    callback close();
    callback window-move();

    callback dispatch-global-event(string);
    callback dispatch-parent-event(string);
    callback dispatch-self-event(string);
}
