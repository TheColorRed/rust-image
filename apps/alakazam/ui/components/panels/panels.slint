import { Layers } from "layers/layers.slint";
import { Colors } from "colors/colors.slint";
export component Panels inherits Rectangle {
    // border-width: 1px;
    // border-color: green;
    VerticalLayout {
        Colors { }

        Layers { }
    }
}
